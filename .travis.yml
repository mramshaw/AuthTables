language: go
go:
  - 1.11.x
  - tip

services:
  - docker

script:
  - docker-compose build
  - docker-compose run authtables go test -race -coverprofile=coverage.txt -covermode=atomic -v
  - docker-compose up -d
  - ./scripts/test

notifications:
  email: false

after_success:
  - bash <(curl -s https://codecov.io/bash)
